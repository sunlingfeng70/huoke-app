Used https://pypi.tuna.tsinghua.edu.cn/simple as the mirror due to PyPI connectivity issues. To persist that mirror for future installs, run uv add --default-index https://pypi.tuna.tsinghua.edu.cn/simple.


uv run streamlit run streamlit_app.py